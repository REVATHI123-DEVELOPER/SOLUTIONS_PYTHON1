!/usr/bin/env python

from collector import collect_pr_workitem_mapping_details


def run():
    # This function gets all the completed PRs that have been raised  and
    # it will populate the database with the PR -- WorkItem mapping.
    # If the PR is already in the DB, it will be ignored.
    # If not, it will be updated in the DB.
    collect_pr_workitem_mapping_details.run()

if __name__ == "__main__":
    run()
